# Quick Demo (60 seconds)

Use this on first visit so users can validate setup fast.

```bash
quickthink ask "Give me a 3-step plan to learn SQL basics" --model qwen2.5:1.5b
```

Optional mode comparison:

```bash
quickthink bench "Design a robust parser for malformed CSV" --model qwen2.5:1.5b --runs 1
```

If you want a visual demo asset for README, record a short terminal capture and export as GIF/MP4.
Recommended tools: `asciinema` + `agg` or `vhs`.
